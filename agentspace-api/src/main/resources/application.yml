spring:
  application:
    name: AgentSpace AI

  profiles:
    active: local

server:
  port: 8080

logging:
  level:
    root: INFO

springdoc:
  api-docs:
    path: /api-docs

  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info